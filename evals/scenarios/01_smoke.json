{
  "name": "Smoke — minimal session round-trip",
  "description": "Confirms the agent route accepts a session, opens an SSE stream, and emits at least one iteration boundary. Run with LLM_MOCK=1 so no provider is required.",
  "request": {
    "prompt": "Say hello and set status to completed.",
    "modelId": "claude-haiku-4-5",
    "classification": "unclassified",
    "maxIterations": 2
  },
  "llmMock": "1",
  "expect": {
    "creationStatus": 200,
    "events": {
      "mustContain": ["session_start", "iteration_start"]
    },
    "piiDetected": false
  }
}
